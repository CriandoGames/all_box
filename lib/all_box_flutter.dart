// This file is obsolete: the optional Flutter reactive layer
// (AllBoxListenable, AllBoxBuilder) was removed as part of the reactivity
// removal breaking change. `all_box` is now a pure-Dart package with a
// single entrypoint: `package:all_box/all_box.dart`.
//
// The sandbox this change was made in could not delete files from the
// mounted filesystem — please delete this file manually
// (lib/all_box_flutter.dart), and remove any `all_box_flutter.dart` import
// from consuming apps in favor of `package:all_box/all_box.dart`.
//
// **PT-BR:** Este arquivo está obsoleto: a camada reativa opcional do
// Flutter (AllBoxListenable, AllBoxBuilder) foi removida como parte da
// quebra de compatibilidade de remoção da reatividade. O `all_box` agora é
// um pacote Dart puro com um único ponto de entrada:
// `package:all_box/all_box.dart`.
//
// O sandbox onde esta mudança foi feita não conseguiu apagar arquivos do
// filesystem montado — por favor apague este arquivo manualmente
// (lib/all_box_flutter.dart), e remova qualquer import de
// `all_box_flutter.dart` em apps consumidores, usando
// `package:all_box/all_box.dart` no lugar.
